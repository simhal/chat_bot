# CLI Tools Environment Configuration
# Copy this file to .env and fill in your values
#
# This .env is for running CLI tools OUTSIDE Docker containers
# It uses localhost connections to reach Docker-exposed database ports
#
# Prerequisites:
#   1. docker-compose.override.yml must exist (exposes DB ports)
#   2. Run: docker-compose up -d postgres redis chroma
#   3. Copy this to .env and configure

# OpenAI API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=""
OPENAI_MODEL=gpt-4o-mini

# Database Configuration (localhost for CLI access to Docker containers)
DATABASE_URL=postgresql://chatbot_user:chatbot_password@localhost:5432/chatbot

# Redis Configuration (localhost for CLI access)
REDIS_URL=redis://localhost:6379/0

# ChromaDB Configuration (localhost for CLI access)
CHROMA_HOST=localhost
CHROMA_PORT=8001

# Google Custom Search API (optional, for research agents)
GOOGLE_API_KEY=""
GOOGLE_SEARCH_ENGINE_ID=""

# LangSmith Observability (optional)
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=""
# LANGCHAIN_PROJECT=chatbot-cli
