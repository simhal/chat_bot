# Local Development Environment Configuration
# Copy this file to .env.local and fill in your values
#
# This .env.local is for running the backend OUTSIDE Docker containers
# while connecting to Docker-exposed database services
#
# Prerequisites:
#   1. docker-compose.override.yml must exist (exposes DB ports)
#   2. Run: docker-compose up -d postgres redis chroma
#   3. Copy this to .env.local and configure
#   4. Run: cd backend && uv run uvicorn main:app --reload

# Development Mode
DEBUG=true

# LinkedIn OAuth Configuration
LINKEDIN_CLIENT_ID=""
LINKEDIN_CLIENT_SECRET=""

# OpenAI ChatGPT API Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=""
# Options: gpt-4o, gpt-4o-mini (recommended), gpt-4-turbo, gpt-3.5-turbo
OPENAI_MODEL=gpt-4o-mini

# Google Custom Search API Configuration (optional)
GOOGLE_API_KEY=""
GOOGLE_SEARCH_ENGINE_ID=""

# CORS Origins - includes SvelteKit dev server (5173) and production (3000)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Database Configuration (localhost for access to Docker containers)
DATABASE_URL=postgresql://chatbot_user:chatbot_password@localhost:5432/chatbot

# Redis Configuration (localhost for access to Docker containers)
REDIS_URL=redis://localhost:6379/0

# ChromaDB Vector Database Configuration (localhost, port 8001 per override)
CHROMA_HOST=localhost
CHROMA_PORT=8001

# JWT Authentication Configuration
JWT_SECRET_KEY="dev-secret-key-change-in-production"
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440
REFRESH_TOKEN_EXPIRE_DAYS=7

# Base URL for resource links in published articles
API_BASE_URL=http://localhost:8000

# Agent Build (v2 is default)
AGENT_BUILD=v2

# LangSmith Observability (Optional)
# LANGCHAIN_TRACING_V2=true
# LANGCHAIN_API_KEY=""
# LANGCHAIN_PROJECT=chatbot-local-dev
